[package]
name = "dull"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "*", features = ["derive"] }
codespan = "*"
codespan-reporting = "*"
rand = "0.8"

[dev-dependencies]
walkdir = "2"
tempfile = "3"
ceres-core = { path = "../PC-1500-Emulator/ceres-core" }